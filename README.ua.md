# 📱 Кишеньковий сервер на Android

Повноцінний сервер на базі смартфона: Python-боти, тунелі, SSH, автозапуск, Zero Trust.

> **Перевірено на Android 13 (Termux з F-Droid).**

---

# ⚠️ Важливі зауваження

### ❗ Використовуй ТІЛЬКИ Termux з F-Droid

Посилання:
[https://f-droid.org/packages/com.termux/](https://f-droid.org/packages/com.termux/)

Версія з Play Market:

* має урізані дозволи
* може зупиняти фон робіт
* порушує роботу SSH та cloudflared
* НЕ підходить як сервер

### ❗ Обмеження Android 12–14

cloudflared може падати → для стабільності використовуй **proot-wrapper**.
Це враховано в інструкції.

---

# 🚀 Розділ 1 — Підготовка Termux

### Встановлення базових пакетів

```bash
# Оновлюємо пакети
pkg update && pkg upgrade

# Встановлюємо Python, Node.js, Git, SSH-сервер, Nano, Proot
pkg install python nodejs git openssh nano proot
```

### Wakelock — захист від зупинення процесів

```bash
# Забороняє Android присипляти Termux
termux-wake-lock
```

Також можна натиснути **Acquire Wakelock** у сповіщенні Termux.

### Вимкнення оптимізації батареї

Налаштування → Додатки → Termux → Батарея →

* **Дозволити фонову активність**
* **Вимкнути оптимізацію батареї**

---

# 🚪 Розділ 2 — SSH на телефоні

```bash
# Встановити пароль для SSH
passwd

# Запустити SSH сервер (порт 8022)
sshd

# Дізнатись логін користувача (u0_a123)
whoami
```

---

# 🌐 Розділ 3 — Zero Trust SSH (Cloudflare)

Найнадійніший спосіб доступу, працює навіть за CG-NAT.

## Встановлення cloudflared

```bash
# Завантажити ARM64 cloudflared
wget https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-arm64

# Дати права на виконання
chmod +x cloudflared-linux-arm64

# Перемістити до системної директорії
mv cloudflared-linux-arm64 $PREFIX/bin/cloudflared
```

### Якщо cloudflared падає (Android 12–14)

```bash
# Запустити тунель через proot
proot -0 cloudflared tunnel run --token <ТОКЕН>
```

---

## Налаштування тунелю у Cloudflare Zero Trust

1. Zero Trust → Access → **Tunnels**
2. Створити тунель
3. Додати публічне доменне імʼя
4. Service:

   * `ssh://localhost:8022`
5. Вибрати метод авторизації (email / OTP / GitHub)
6. Скопіювати **Tunnel Token**

---

## Тестовий запуск тунелю

```bash
# Стандартний запуск
cloudflared tunnel run --token <ТОКЕН>
```

Або:

```bash
# Надійний варіант під Android 12–14 (фікс шляхів)
termux-chroot cloudflared tunnel run --token <ТОКЕН>
```

---

## Автостарт через PM2

```bash
# Встановити PM2
npm install pm2 -g

# Запустити cloudflared як сервіс (використовуємо termux-chroot для стабільності)
pm2 start termux-chroot --name "phone" -- cloudflared tunnel run --protocol http2 --token <ТОКЕН>

# Зберегти автозапуск
pm2 save
```

---

## Підключення з компʼютера

```bash
# Авторизація пристрою у Zero Trust (якщо потрібно)
cloudflared access login phone.tvij-domen.com
```

Конфіг `~/.ssh/config`:

```text
Host phone
  HostName phone.tvij-domen.com
  User u0_a...  # Результат виконання whoami
  ProxyCommand cloudflared access ssh --hostname %h
```

Підключення:

```bash
ssh phone
```

---

# ⚙️ Розділ 4 — PM2 для ботів

```bash
# Запустити Python-бота
pm2 start bot.py --interpreter python --name mybot

# Зберегти процеси
pm2 save
```

Корисні команди:

* `pm2 logs` — логи контейнера
* `pm2 list` — Список контейнерів
* `pm2 restart mybot` — Рестарт (в данному випадку) бота
* `pm2 resurrect` — Підняття збереженого стану після перезавантаження

---

# 🧹 Розділ 5 — Logrotate

```bash
# Встановити модуль ротації логів
pm2 install pm2-logrotate

# Ліміт розміру файлу (10 МБ)
pm2 set pm2-logrotate:max_size 10M

# Ротація по Понеділках і Четвергах
pm2 set pm2-logrotate:rotateInterval '0 0 * * 1,4'

# Зберігати тільки 3 останні архіви
pm2 set pm2-logrotate:retain 3
pm2 set pm2-logrotate:compress true
```

---

# 🔐 Розділ 6 — .env приклад

```
BOT_TOKEN=123
GROQ_API_KEY=gsk...
ADMIN_ID=123456
```

Python:

```python
from dotenv import load_dotenv
import os
load_dotenv()
token = os.getenv("BOT_TOKEN")
```

---

# ✅ Готово

Твій смартфон тепер — повноцінний **кишеньковий сервер з Zero Trust захистом**, стабільним SSH-тунелем, логуванням та автозапуском.
